	[파일입출력]
		- 저장공간
		- 변수 < 배열 < 객체(클래스) < 콜렉션프레임워크 < 파일 < DB
		
		[1] 파일입출력
			- 메모장, 엑셀, 워드 등의 파일을 가져오고 쓸 수 있다.
			- ★ 파일시스템설정시 로그파일 남길 때 필수 사용
			- Reader : stream(파일흐름)을 읽어오는 기능
			- Writer : stream(파일흐름)을 쓰는 기능
			
			- csv
			- xml
			- json