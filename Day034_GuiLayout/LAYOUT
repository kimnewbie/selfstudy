	ㅁ LAYOUT
	1) FlowLayout
		- 기본적으로 컴포넌트들이 왼쪽에서 오른쪽으로 추가되는 형태
		- container의 크기가 변경되면 컴포넌트는 고정되고 위치는 변경
		- 기본 중앙정렬
		- 사용방법 frame.setLayout(new FlowLayout());
	2) BorderLayout
		- 컴포넌트들이 상(North), 하(South), 좌(East), 우(West), 중간(Center)에 배치
		- 컴포넌트의 영역을 지정하지 않을 경우 자동으로 중간 영역에 배치
		- 같은 위치에 컴포넌트들이 추가되면, 뒤 컴포넌트에 가려서 앞의 컴포넌트가 보이지 않음.
		- BoarderLayout에 컴포넌트를 추가할 때는	.add(컴포넌트,위치)
		- 사용방법 frame.setLayout(new BoarderLayout());
		
			add(new JButton("Center"));
	        add(new JButton("West"),BorderLayout.LINE_START);
	        add(new JButton("East"),BorderLayout.LINE_END);
	        add(new JButton("North"),BorderLayout.PAGE_START);
	        add(new JButton("South"),BorderLayout.PAGE_END);
	        
	3) GridLayout
		- 테이블형태의 레이아웃
		- 행과 열로 관리된다.
		- 인수를 주지 않으면 1행으로 고정, 열이 계속 추가됨(무한대).
		- 사용방법 frame.setLayout(new GridLayout(2,3));
	4) CardLayout
		- 여러 장의 카드처럼 컨테이너를 배치하여 각각의 컨테이너 위에 컴포넌트(JComponent)를 배치
		- 사용방법 frame.setLayout(new CardLayout());